# Task 42

Run `data_processing.py` to clean up the data and generate the `nodes.csv` and `edges.csv` files for each city.

Run `data_analysis.py` to generate plots (stored in `figures/`) and compute other quantities.